{% set name = "word-image" %}
{% set version = "0.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/word_image-{{ version }}.tar.gz
  sha256: 2f9434684134860e1feb7378881be86982e1722d9bb304babec30efe9ad6c89f

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 2

requirements:
  build:
    - {{ compiler('c') }}
    - xorg-libxfixes  # [linux]
  host:
    - pip
    - python
    - libgl-devel                    # [linux]
  run:
    - imgaug
    - matplotlib-base
    - numpy
    - pillow
    - python

test:
  imports:
    - word_image
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/econser/word_image
  summary: Generate images of words with configurable augmentation options
  license: MIT
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - econser
